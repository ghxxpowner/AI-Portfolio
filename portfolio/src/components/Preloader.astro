<div class="preloader" data-preloader aria-hidden="true">
  <p class="preloader-mark">
    <span class="preloader-spark"></span>
    Daniel Klinke
  </p>
</div>

<style>
  /* Invisible unless the inline head snippet opted this page-load in.
     Crawlers, no-JS visitors and reduced-motion users never see it. */
  .preloader {
    display: none;
  }

  :global(html[data-preload]) .preloader {
    position: fixed;
    inset: 0;
    z-index: 200;
    display: grid;
    place-items: center;
    background-color: var(--color-bg);
    opacity: 1;
    transition: opacity 350ms var(--ease-out-cubic) 450ms;
    /* CSS-only failsafe: if the module bundle never runs (blocked, failed),
       the overlay dismisses itself without any JavaScript. */
    animation: preloader-expire 400ms var(--ease-out-cubic) 2.2s forwards;
  }

  :global(html[data-preload]) .preloader.is-playing {
    opacity: 0;
  }

  @keyframes preloader-expire {
    to {
      opacity: 0;
      visibility: hidden;
    }
  }

  .preloader-mark {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--text-xl);
    font-weight: 650;
    letter-spacing: var(--tracking-tight);
    opacity: 0;
    animation: preloader-in 420ms var(--ease-out-cubic) forwards;
  }

  .preloader-spark {
    inline-size: 0.9em;
    block-size: 0.9em;
    background-color: var(--color-accent);
    border-radius: var(--radius-sm);
    rotate: 45deg;
    animation: preloader-spin 900ms var(--ease-out-cubic) infinite;
  }

  @keyframes preloader-in {
    from {
      opacity: 0;
      translate: 0 0.5rem;
    }
    to {
      opacity: 1;
      translate: 0 0;
    }
  }

  @keyframes preloader-spin {
    to {
      rotate: 225deg;
    }
  }
</style>
